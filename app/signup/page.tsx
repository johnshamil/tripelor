"use client";
import Link from "next/link";
import { useState } from "react";
import { UserPlus } from "lucide-react";
export default function SignupPage(){const[fullName,setFullName]=useState("");const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[status,setStatus]=useState("");const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setStatus("");try{const r=await fetch("/api/auth/signup",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({fullName,email,password})});const x=await r.json();if(!r.ok)throw new Error(x.error||"Unable to create account.");setStatus(x.needsConfirmation?"Account created. Please check your email and confirm your address before logging in.":"Account created successfully. You can now log in.");}catch(e){setStatus(e instanceof Error?e.message:"Unable to create account.");}finally{setBusy(false)}}return <main className="container py-16"><div className="mx-auto max-w-md"><p className="text-sm uppercase tracking-[.3em] text-gold">Join Tripelor</p><h1 className="mt-2 text-4xl font-bold">Create your account</h1><p className="mt-3 text-gray-400">Keep your Maldives bookings in one place.</p><form onSubmit={submit} className="card mt-8 grid gap-5 p-6 md:p-8"><input required value={fullName} onChange={e=>setFullName(e.target.value)} placeholder="Full name" className="rounded-xl border border-white/10 bg-black px-4 py-3"/><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email address" className="rounded-xl border border-white/10 bg-black px-4 py-3"/><input required minLength={8} type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password (minimum 8 characters)" className="rounded-xl border border-white/10 bg-black px-4 py-3"/>{status&&<p className="rounded-xl border border-white/10 bg-white/[.04] p-3 text-sm">{status}</p>}<button disabled={busy} className="btn-gold gap-2 disabled:opacity-60"><UserPlus className="h-4 w-4"/>{busy?"Creating...":"Create Account"}</button><p className="text-sm text-gray-400">Already have an account? <Link href="/login" className="text-gold">Log in</Link></p></form></div></main>}
