import Link from "next/link";
import { CheckCircle2, Mail, MessageCircle } from "lucide-react";

export default function ConfirmationPage({searchParams}:{searchParams?:{name?:string;title?:string;dates?:string}}){return <main className="container py-20"><div className="mx-auto max-w-2xl card border-gold/30 p-8 text-center md:p-12"><div className="mx-auto flex h-16 w-16 items-center justify-center rounded-full bg-gold/10"><CheckCircle2 className="h-9 w-9 text-gold"/></div><p className="mt-6 text-sm uppercase tracking-[.3em] text-gold">Request Received</p><h1 className="mt-3 text-4xl font-bold">Thank you{searchParams?.name?`, ${searchParams.name}`:""}.</h1><p className="mt-5 leading-7 text-gray-300">Your Tripelor booking request has been sent successfully. We&apos;ll check availability and send the final confirmation to your email.</p>{searchParams?.title&&<div className="mt-7 rounded-2xl border border-white/10 bg-white/[.03] p-5 text-left"><p className="text-xs uppercase tracking-[.2em] text-gray-500">Booking</p><p className="mt-2 text-xl font-semibold">{searchParams.title}</p>{searchParams?.dates&&<p className="mt-2 text-sm text-gray-400">{searchParams.dates}</p>}</div>}<div className="mt-8 grid gap-3 sm:grid-cols-2"><div className="rounded-2xl border border-white/10 p-4"><Mail className="mx-auto h-5 w-5 text-gold"/><p className="mt-2 text-sm">Check your email for your Tripelor receipt.</p></div><div className="rounded-2xl border border-white/10 p-4"><MessageCircle className="mx-auto h-5 w-5 text-gold"/><p className="mt-2 text-sm">Need help? Chat with us on WhatsApp.</p></div></div><div className="mt-8 flex flex-wrap justify-center gap-3"><Link href="/" className="btn-outline">Back Home</Link><Link href="/speedboat" className="btn-gold">Arrange Speedboat</Link></div></div></main>}
