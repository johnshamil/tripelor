"use client";

import { useMemo, useState } from "react";
import Link from "next/link";
import { BedDouble, CalendarDays, CheckCircle2, Ship, Sparkles } from "lucide-react";

const packageOptions=[
  {name:"Maldives Reef & Relax Escape",nights:3,price:500,meal:"Half Board"},{name:"Island Adventure",nights:3,price:650,meal:"Half Board"},{name:"Maldives Ocean Discovery Escape",nights:3,price:750,meal:"Full Board"},{name:"Beach BBQ Dinner Escape",nights:3,price:650,meal:"Half Board"},{name:"Honeymoon Island Escape",nights:3,price:900,meal:"Full Board"},{name:"Manta & Dolphin Adventure",nights:3,price:700,meal:"Half Board"},{name:"Sunset & Sandbank Escape",nights:3,price:550,meal:"Half Board"},
  {name:"Maldives Reef & Relax Escape",nights:5,price:750,meal:"Half Board"},{name:"5-Night Island Adventure",nights:5,price:950,meal:"Half Board"},{name:"Maldives Ocean Discovery Escape",nights:5,price:1150,meal:"Full Board"},{name:"Beach BBQ Dinner Escape",nights:5,price:990,meal:"Half Board"},{name:"Honeymoon Island Escape",nights:5,price:1350,meal:"Full Board"},{name:"Manta & Dolphin Adventure",nights:5,price:1050,meal:"Half Board"},{name:"Sunset & Sandbank Escape",nights:5,price:820,meal:"Half Board"}
];

export default function BuildYourTripPage(){
  const [nights,setNights]=useState(3);const [room,setRoom]=useState("ROOM 101");const [selected,setSelected]=useState(0);const [seats,setSeats]=useState(2);
  const options=packageOptions.filter(x=>x.nights===nights);const pkg=options[Math.min(selected,options.length-1)]||options[0];
  const speedboat=seats*50;const total=(pkg?.price||0)+speedboat;
  const bookingHref=useMemo(()=>pkg?`/booking?package=${encodeURIComponent(pkg.name+" - USD "+pkg.price)}&mealPlan=${encodeURIComponent(pkg.meal)}&nights=${pkg.nights}&property=${encodeURIComponent("Uhoo's Lavish Oasis")}&roomType=${encodeURIComponent(room)}`:"/booking",[pkg,room]);
  return <main className="container py-14 md:py-20"><div className="mx-auto max-w-5xl"><p className="text-sm uppercase tracking-[.32em] text-gold">Tripelor Trip Builder</p><h1 className="mt-3 text-4xl font-bold md:text-6xl">Build your Maldives escape</h1><p className="mt-5 max-w-3xl text-gray-400">Choose your package, room and speedboat seats. Tripelor calculates one estimated total before you send the booking request.</p>
  <div className="mt-10 grid gap-6 lg:grid-cols-[1.2fr_.8fr]"><section className="card p-6 md:p-8"><div className="grid gap-6">
    <div><p className="mb-3 flex items-center gap-2 font-semibold"><CalendarDays className="h-5 w-5 text-gold"/>Trip length</p><div className="grid grid-cols-2 gap-3">{[3,5].map(n=><button key={n} onClick={()=>{setNights(n);setSelected(0)}} className={`rounded-2xl border px-5 py-4 text-left transition ${nights===n?"border-gold bg-gold/10 text-gold":"border-white/10 hover:border-gold/40"}`}><strong className="text-xl">{n} Nights</strong><span className="mt-1 block text-xs text-gray-400">Couple package · 2 adults</span></button>)}</div></div>
    <label className="grid gap-2"><span className="flex items-center gap-2 font-semibold"><Sparkles className="h-5 w-5 text-gold"/>Package</span><select value={selected} onChange={e=>setSelected(Number(e.target.value))} className="rounded-xl border border-white/10 bg-black px-4 py-3">{options.map((x,i)=><option key={`${x.name}-${x.nights}`} value={i}>{x.name} — USD {x.price}</option>)}</select></label>
    <label className="grid gap-2"><span className="flex items-center gap-2 font-semibold"><BedDouble className="h-5 w-5 text-gold"/>Room at Uhoo&apos;s Lavish Oasis</span><select value={room} onChange={e=>setRoom(e.target.value)} className="rounded-xl border border-white/10 bg-black px-4 py-3"><option>ROOM 101</option><option>ROOM 102</option></select></label>
    <label className="grid gap-2"><span className="flex items-center gap-2 font-semibold"><Ship className="h-5 w-5 text-gold"/>Speedboat seats</span><select value={seats} onChange={e=>setSeats(Number(e.target.value))} className="rounded-xl border border-white/10 bg-black px-4 py-3">{Array.from({length:10},(_,i)=>i+1).map(n=><option key={n} value={n}>{n} seat{n>1?"s":""} — USD {n*50}</option>)}</select><span className="text-xs text-gray-500">USD 50 per person. Transfer requests must be made at least 24 hours before arrival.</span></label>
  </div></section>
  <aside className="card h-fit border-gold/30 p-6 md:p-8"><p className="text-xs uppercase tracking-[.25em] text-gold">Your Tripelor plan</p><h2 className="mt-2 text-2xl font-bold">{pkg?.name}</h2><div className="mt-6 space-y-3 text-sm">{[`Uhoo's Lavish Oasis · ${room}`,`${nights} nights · 2 adults`,`${pkg?.meal} meal plan`,`${seats} speedboat seat${seats>1?"s":""}`].map(x=><p key={x} className="flex items-start gap-2 text-gray-300"><CheckCircle2 className="mt-0.5 h-4 w-4 shrink-0 text-gold"/>{x}</p>)}</div><div className="mt-7 border-t border-white/10 pt-6"><div className="flex justify-between text-sm text-gray-400"><span>Package</span><span>USD {pkg?.price}</span></div><div className="mt-2 flex justify-between text-sm text-gray-400"><span>Speedboat</span><span>USD {speedboat}</span></div><div className="mt-4 flex items-end justify-between"><span className="font-semibold">Estimated total</span><strong className="text-4xl text-gold">USD {total}</strong></div><p className="mt-3 text-xs text-gray-500">Final availability, excursion conditions and booking confirmation are provided by Tripelor before payment.</p><Link href={bookingHref} className="btn-gold mt-6 w-full">Continue to Booking</Link></div></aside>
  </div></div></main>;
}
