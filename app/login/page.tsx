"use client";
import Link from "next/link";
import { useState } from "react";
import { LogIn } from "lucide-react";
export default function LoginPage(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[status,setStatus]=useState("");const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setStatus("");try{const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});const x=await r.json();if(!r.ok)throw new Error(x.error||"Unable to log in.");window.location.href="/account";}catch(e){setStatus(e instanceof Error?e.message:"Unable to log in.");}finally{setBusy(false)}}return <main className="container py-16"><div className="mx-auto max-w-md"><p className="text-sm uppercase tracking-[.3em] text-gold">Tripelor Account</p><h1 className="mt-2 text-4xl font-bold">Welcome back</h1><p className="mt-3 text-gray-400">Log in to view your Tripelor bookings and account details.</p><form onSubmit={submit} className="card mt-8 grid gap-5 p-6 md:p-8"><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email address" className="rounded-xl border border-white/10 bg-black px-4 py-3"/><input required type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password" className="rounded-xl border border-white/10 bg-black px-4 py-3"/>{status&&<p className="rounded-xl border border-white/10 bg-white/[.04] p-3 text-sm">{status}</p>}<button disabled={busy} className="btn-gold gap-2 disabled:opacity-60"><LogIn className="h-4 w-4"/>{busy?"Logging in...":"Log In"}</button><div className="flex justify-between gap-4 text-sm"><Link href="/signup" className="text-gold">Create account</Link><Link href="/account/forgot-password" className="text-gray-400 hover:text-gold">Forgot password?</Link></div></form></div></main>}
