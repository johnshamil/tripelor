"use client";
import Link from "next/link";
import { useState } from "react";
export default function ForgotPassword(){const[email,setEmail]=useState("");const[status,setStatus]=useState("");const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setStatus("");try{const r=await fetch("/api/auth/forgot",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email})});const x=await r.json();if(!r.ok)throw new Error(x.error||"Unable to send reset email.");setStatus("Password reset email sent. Please check your inbox.");}catch(e){setStatus(e instanceof Error?e.message:"Unable to send reset email.");}finally{setBusy(false)}}return <main className="container py-20"><div className="mx-auto max-w-md"><p className="text-sm uppercase tracking-[.3em] text-gold">Account Recovery</p><h1 className="mt-2 text-4xl font-bold">Reset your password</h1><form onSubmit={submit} className="card mt-8 grid gap-5 p-6"><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Your email address" className="rounded-xl border border-white/10 bg-black px-4 py-3"/>{status&&<p className="rounded-xl border border-white/10 p-3 text-sm">{status}</p>}<button disabled={busy} className="btn-gold">{busy?"Sending...":"Send Reset Link"}</button><Link href="/login" className="text-center text-sm text-gray-400 hover:text-gold">Back to login</Link></form></div></main>}
