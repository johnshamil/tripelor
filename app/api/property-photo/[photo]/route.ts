import { propertyConfig, propertyDB } from "@/lib/property-store";
import { currentUser, isAdminEmail } from "@/lib/auth-server";
export const dynamic="force-dynamic";
export async function GET(r:Request,{params}:{params:{photo:string}}){try{if(!/^[0-9a-f-]{36}\.(jpg|png|webp)$/.test(params.photo))return new Response(null,{status:404});const rows=await propertyDB("managed_properties?select=id,data&status=eq.published");const listed=rows.some((row:any)=>Array.isArray(row.data?.photos)&&row.data.photos.includes(params.photo));if(!listed){const u=await currentUser();if(!u||!isAdminEmail(u.email))return new Response(null,{status:404});}const {url,key}=propertyConfig();const image=await fetch(`${url}/storage/v1/object/property-photos/${params.photo}`,{headers:{apikey:key,Authorization:`Bearer ${key}`},cache:"no-store"});if(!image.ok)return new Response(null,{status:404});return new Response(image.body,{headers:{"Content-Type":image.headers.get("Content-Type")||"image/jpeg","Cache-Control":"private, no-store","X-Content-Type-Options":"nosniff"}});}catch{return new Response(null,{status:503});}}
