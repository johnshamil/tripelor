const OWNER="bookings@tripelor.com";function esc(v:unknown){return String(v??"").replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/\"/g,"&quot;").replace(/'/g,"&#039;")};export async function POST(req:Request){try{const key=process.env.RESEND_API_KEY;if(!key)return Response.json({error:"Review service is not configured."},{status:500});const b=await req.json();if(!b.name||!b.email||!b.country||!b.rating||!b.review||b.permission!=="yes")return Response.json({error:"Please complete all required fields and publication permission."},{status:400});const rating=Math.max(1,Math.min(5,Number(b.rating)||0));const html=`<div style="font-family:Arial,sans-serif;max-width:700px;margin:auto"><h1>New Tripelor Guest Review</h1><p><b>Status:</b> Pending your approval — this review is NOT automatically published.</p><hr><p><b>Guest:</b> ${esc(b.name)}</p><p><b>Email (private):</b> ${esc(b.email)}</p><p><b>Country:</b> ${esc(b.country)}</p><p><b>Booking:</b> ${esc(b.booking)}</p><p><b>Travel month:</b> ${esc(b.travelMonth||"Not provided")}</p><p><b>Rating:</b> ${"★".repeat(rating)}${"☆".repeat(5-rating)}</p><h3>Review</h3><p>${esc(b.review).replace(/\n/g,"<br>")}</p><hr><p><b>Publication permission:</b> Yes</p></div>`;const r=await fetch("https://api.resend.com/emails",{method:"POST",headers:{Authorization:`Bearer ${key}`,"Content-Type":"application/json"},body:JSON.stringify({from:"Tripelor Reviews <onboarding@resend.dev>",to:[OWNER],reply_to:b.email,subject:`New ${rating}-star Tripelor review - ${esc(b.name)}`,html})});const x=await r.json();if(!r.ok)return Response.json({error:x?.message||"Unable to submit review."},{status:500});return Response.json({success:true})}catch(e){console.error(e);return Response.json({error:"Something went wrong while submitting your review."},{status:500})}}
