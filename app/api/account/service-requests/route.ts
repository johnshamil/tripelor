import {requireUser} from "@/lib/auth-server";
function cfg(){const url=process.env.SUPABASE_URL?.replace(/\/$/,"");const key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)throw new Error("Guest service database is not configured.");return{url,key}}
export async function GET(){try{const user=await requireUser();const{url,key}=cfg();const email=encodeURIComponent(String(user.email||"").toLowerCase());const r=await fetch(`${url}/rest/v1/guest_service_requests?select=*&guest_email=eq.${email}&order=created_at.desc&limit=50`,{headers:{apikey:key,Authorization:`Bearer ${key}`},cache:"no-store"});const t=await r.text();const data=t?JSON.parse(t):[];if(!r.ok)throw new Error(data?.message||"Unable to load requests.");return Response.json({requests:data})}catch(e){if(e instanceof Error&&e.message==="UNAUTHORIZED")return Response.json({error:"Please log in."},{status:401});return Response.json({error:e instanceof Error?e.message:"Unable to load requests."},{status:500})}}
export async function POST(request:Request){try{const user=await requireUser();const{url,key}=cfg();const b=await request.json();const allowed=["breakfast","housekeeping","snorkeling","excursion","late_checkout","speedboat","other"];if(!allowed.includes(b.requestType))return Response.json({error:"Invalid request type."},{status:400});const payload={guest_email:String(user.email||"").toLowerCase(),reservation_id:b.reservationId||null,property_name:b.propertyName||null,room_type:b.roomType||null,request_type:b.requestType,details:String(b.details||"").trim()||null,requested_for:b.requestedFor||null,status:"pending"};const r=await fetch(`${url}/rest/v1/guest_service_requests`,{method:"POST",headers:{apikey:key,Authorization:`Bearer ${key}`,"Content-Type":"application/json",Prefer:"return=representation"},body:JSON.stringify(payload)});const t=await r.text();const data=t?JSON.parse(t):[];if(!r.ok)throw new Error(data?.message||"Unable to send request.");return Response.json({success:true,request:data?.[0]||null})}catch(e){if(e instanceof Error&&e.message==="UNAUTHORIZED")return Response.json({error:"Please log in."},{status:401});return Response.json({error:e instanceof Error?e.message:"Unable to send request."},{status:500})}}
