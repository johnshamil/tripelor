"use client";
import {useEffect,useMemo,useState} from "react";
import {CalendarDays,ChevronLeft,ChevronRight,X} from "lucide-react";

const iso=(d:Date)=>`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}-${String(d.getDate()).padStart(2,"0")}`;
const pretty=(v:string)=>v?new Date(`${v}T00:00:00`).toLocaleDateString("en-GB",{day:"2-digit",month:"short",year:"numeric"}):"Select date";

export default function AvailabilityDatePicker({label,value,onChange,propertyName,roomType,minDate,disabled=false}:{label:string;value:string;onChange:(v:string)=>void;propertyName:string;roomType:string;minDate?:string;disabled?:boolean}){
 const[open,setOpen]=useState(false);const initial=value?new Date(`${value}T00:00:00`):new Date();const[month,setMonth]=useState(()=>new Date(initial.getFullYear(),initial.getMonth(),1));const[days,setDays]=useState<Record<string,boolean>>({});const[loading,setLoading]=useState(false);const start=new Date(month.getFullYear(),month.getMonth(),1),end=new Date(month.getFullYear(),month.getMonth()+1,0);const today=iso(new Date());
 useEffect(()=>{if(!open)return;setLoading(true);fetch(`/api/availability/calendar?propertyName=${encodeURIComponent(propertyName)}&roomType=${encodeURIComponent(roomType)}&from=${iso(start)}&to=${iso(end)}`,{cache:"no-store"}).then(r=>r.json()).then(x=>{const map:Record<string,boolean>={};for(const d of x.days||[])map[d.day]=Boolean(d.available);setDays(map)}).catch(()=>setDays({})).finally(()=>setLoading(false))},[open,propertyName,roomType,month.getFullYear(),month.getMonth()]);
 useEffect(()=>{if(value){const d=new Date(`${value}T00:00:00`);setMonth(new Date(d.getFullYear(),d.getMonth(),1))}},[value]);
 const cells=useMemo(()=>{const a:(Date|null)[]=Array(start.getDay()).fill(null);for(let i=1;i<=end.getDate();i++)a.push(new Date(month.getFullYear(),month.getMonth(),i));return a},[month.getFullYear(),month.getMonth()]);
 function choose(k:string,available:boolean,past:boolean){if(!available||past||disabled)return;onChange(k);setOpen(false)}
 return <div className="grid gap-2 text-sm"><span className="flex items-center gap-2"><CalendarDays className="h-4 w-4 text-gold"/>{label}</span><button type="button" disabled={disabled} onClick={()=>setOpen(true)} className="flex items-center justify-between rounded-xl border border-white/10 bg-black px-4 py-3 text-left disabled:opacity-60"><span className={value?"text-white":"text-gray-500"}>{pretty(value)}</span><CalendarDays className="h-4 w-4 text-gold"/></button>{open&&<div className="fixed inset-0 z-[100] flex items-center justify-center bg-black/75 p-4 backdrop-blur-sm" onClick={()=>setOpen(false)}><div className="w-full max-w-md overflow-hidden rounded-2xl border border-white/15 bg-[#121212] shadow-2xl" onClick={e=>e.stopPropagation()}><div className="flex items-start justify-between bg-gradient-to-br from-zinc-600 to-zinc-700 p-6"><div><p className="text-sm text-gray-200">{month.getFullYear()}</p><p className="mt-1 text-3xl font-semibold text-white">{value?new Date(`${value}T00:00:00`).toLocaleDateString("en-US",{weekday:"short",day:"numeric",month:"short"}):label}</p><p className="mt-2 text-xs text-gray-300">{roomType}</p></div><button type="button" onClick={()=>setOpen(false)} className="rounded-full p-2 text-white hover:bg-white/10"><X className="h-5 w-5"/></button></div><div className="p-5"><div className="flex items-center justify-between"><button type="button" onClick={()=>setMonth(new Date(month.getFullYear(),month.getMonth()-1,1))} className="rounded-full p-2 hover:bg-white/10"><ChevronLeft/></button><strong>{month.toLocaleDateString("en-US",{month:"long",year:"numeric"})}</strong><button type="button" onClick={()=>setMonth(new Date(month.getFullYear(),month.getMonth()+1,1))} className="rounded-full p-2 hover:bg-white/10"><ChevronRight/></button></div><div className="mt-5 grid grid-cols-7 gap-1 text-center text-xs text-gray-500">{["S","M","T","W","T","F","S"].map((x,i)=><span key={`${x}${i}`}>{x}</span>)}</div><div className="mt-2 grid grid-cols-7 gap-1">{cells.map((d,i)=>{if(!d)return <span key={`e${i}`} className="aspect-square"/>;const k=iso(d),past=k<today||(minDate? k<minDate:false),available=days[k]!==false,selected=value===k;return <button key={k} type="button" disabled={past||!available||loading} onClick={()=>choose(k,available,past)} className={`aspect-square rounded-full text-sm font-semibold transition ${past?"text-gray-700":!available?"bg-red-500 text-white cursor-not-allowed":selected?"ring-2 ring-white bg-green-500 text-black":"bg-green-500/20 text-green-300 hover:bg-green-500 hover:text-black"}`}>{d.getDate()}</button>})}</div><div className="mt-5 flex flex-wrap justify-center gap-5 border-t border-white/10 pt-4 text-xs"><span className="flex items-center gap-2"><i className="h-3 w-3 rounded-full bg-green-500"/>Available</span><span className="flex items-center gap-2"><i className="h-3 w-3 rounded-full bg-red-500"/>Booked</span><span className="flex items-center gap-2"><i className="h-3 w-3 rounded-full bg-gray-700"/>Past</span></div><div className="mt-4 flex justify-end gap-3"><button type="button" onClick={()=>setOpen(false)} className="px-4 py-2 text-sm text-blue-400">Cancel</button></div></div></div></div>}</div>;
}
